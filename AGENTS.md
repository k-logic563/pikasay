# pikasay 開発エージェント指示書

## 目的

このリポジトリは、ナキウサギがターミナル上で一言しゃべるネタ系CLI「pikasay」を開発するためのものである。
`cowsay`に着想を得つつ、犬や猫とは異なる、少しマイナーで可愛い実在動物を主役にする。

## 作業開始時の必須手順

1. この `AGENTS.md` を読む。
2. `REQUIREMENTS.md` を読む。
3. `docs/IMPLEMENTATION_PLAN.md` を読む。
4. リポジトリ内の既存コード、`package.json`、設定ファイル、テストを確認する。
5. 現在のフェーズと未完了タスクを特定してから作業する。

既存の実装・設定・ドキュメントがある場合は、それらを優先して扱う。既存仕様と矛盾する変更を独断で行わない。

## 基本方針

- MVPを小さく完成させ、動作確認してから拡張する。
- ネタ感は保ちつつ、CLIとしての使いやすさと壊れにくさを優先する。
- 出力は標準出力、エラーは標準エラー出力へ分ける。
- パイプ入力、引数入力、対話的でない実行を前提にする。
- 端末幅、改行、空入力、Unicode表示幅を意識する。
- 外部APIやネットワーク接続をMVPの実行時依存にしない。
- 依存パッケージは必要最小限にする。
- README、ヘルプ、エラーメッセージは原則として日本語で分かりやすくする。
- 動物の見た目や文言は、可愛さを保ちつつ過度に幼児向けにしない。

## 技術方針

- npmで配布可能なNode.js CLIとして実装する。
- リポジトリに既存の言語・ビルド構成がある場合はそれを尊重する。
- 新規構成の場合は、保守しやすく型安全な構成を優先する。
- CLIの実行エントリ、ビルド、テスト、lint、package公開時のファイル構成を明示する。
- `npm pack --dry-run`で不要ファイルが含まれていないことを確認する。
- `npm test`、`npm run build`など、実際に存在するスクリプトを使って検証する。

## 実装ルール

- 1回の変更の目的を明確にし、無関係なリファクタリングを混ぜない。
- 仕様にない機能を追加する場合は、先に`REQUIREMENTS.md`または実装計画を更新する。
- 破壊的変更を行う場合は、影響範囲と移行方法を記録する。
- シェルコマンドの入力をそのまま実行しない。pikasayは入力を表示するだけにする。
- 端末制御シーケンスや色付けを使う場合は、無効化できる方法を用意する。
- 色や装飾が使えない環境でも、AAと文章だけで意味が伝わるようにする。
- 失敗時もスタックトレースを通常出力へ表示しない。開発時に必要な情報は適切に標準エラーへ出す。

## テスト方針

最低限、以下を自動テストする。

- 引数からメッセージを受け取れる。
- 標準入力からメッセージを受け取れる。
- 引数と標準入力の優先順位が仕様どおりである。
- 空入力を安全に扱える。
- 改行を含む入力を安全に扱える。
- 成功・通常・警告・失敗などの表情またはモードを正しく切り替えられる。
- `--help`と`--version`が動作する。
- 不正なオプションで、利用者に分かるエラーを返す。
- 出力が決められた最大幅を超えない。

実装後は、可能な範囲で以下も確認する。

```bash
printf 'テスト成功\n' | pikasay
pikasay 'デプロイ完了'
pikasay --help
pikasay --version
npm test
npm run build
npm pack --dry-run
```

## 作業報告

作業終了時は、次の順で簡潔に報告する。

1. 変更したファイル
2. 実装した内容
3. 実行した検証コマンドと結果
4. 未対応事項または判断が必要な事項

実装計画のフェーズを完了した場合は、`docs/IMPLEMENTATION_PLAN.md`の状態も更新する。
