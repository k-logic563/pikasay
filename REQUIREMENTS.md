# pikasay 要件定義

## 1. プロジェクト概要

### 1.1 名称

- パッケージ名候補: `pikasay`
- CLIコマンド名: `pikasay`
- モチーフ: ナキウサギ（実在する動物）

### 1.2 コンセプト

小さくて可愛いナキウサギが、開発者の入力に対してターミナル上で一言返す。
`cowsay`のように簡単に使えるが、ナキウサギらしい表情と鳴き声で、成功・注意・失敗の場面を少し楽しくする。

## 2. MVPの範囲

MVPでは、次の機能を完成させる。

- メッセージの表示
- ナキウサギのAA表示
- 標準入力からの受け取り
- コマンドライン引数からの受け取り
- 通常、成功、警告、失敗の表情またはモード
- `--help`、`--version`
- 日本語と英語を含む一般的なUnicode文字の表示
- npmパッケージとしてのビルドと実行
- 自動テスト
- READMEによる利用方法の説明

## 3. 入力仕様

### 3.1 引数入力

```bash
pikasay "テスト成功"
```

複数の引数は、自然な空白区切りで1つのメッセージに結合する。

### 3.2 標準入力

```bash
echo "テスト成功" | pikasay
```

標準入力が利用可能で、メッセージ引数が指定されていない場合は、標準入力を読み取る。
末尾の改行は表示上不要な範囲で整理する。ただし、入力中の意味のある改行は勝手に1行へ潰さない。

### 3.3 優先順位

メッセージ引数が指定されている場合は引数を優先し、標準入力を待ち続けない。
入力がない場合は、短い使い方の案内を表示して、環境に適した終了コードを返す。

## 4. モード仕様

MVPでは、最低限次のモードを提供する。オプション名は実装時に一般的で一貫したものを選ぶ。

| モード | 用途 | 表情・雰囲気の例 |
|---|---|---|
| normal | 通常の一言 | きょとん、穏やか |
| success | 成功報告 | うれしそう、「ぴ！」 |
| warning | 注意や保留 | 少し不安そう、「ぴぃ…」 |
| error | 失敗報告 | 困った表情、「ぴぎゃー！」 |

例:

```bash
pikasay --mood success "テスト、通ったよ"
pikasay --mood warning "未コミットの変更があります"
pikasay --mood error "ビルドに失敗しました"
```

モード未指定時は`normal`とする。モード名はエイリアスを増やしすぎず、ヘルプに明記する。

## 5. 出力仕様

基本形は、ナキウサギのAAと吹き出し風メッセージの組み合わせとする。

```text
   /\\_/\\
  ( •ᴗ• )  ぴ！
  / >🍃

  テスト、通ったよ。
```

上記は方向性を示す例であり、AAは実装時に見た目・端末幅・表示幅を確認して調整する。

- ナキウサギであることが分かる。
- 小さく丸い、可愛い印象を維持する。
- モードごとに表情または短い鳴き声を変える。
- 長文は端末上で崩れにくいよう、適切に折り返す。
- ANSIカラーは任意。無効化しても意味が失われない。
- 色付き出力を使う場合は、`NO_COLOR`または同等の無効化方法を尊重する。
- 出力に入力内容をコードとして解釈する機能は持たせない。

## 6. CLIオプション

MVPで最低限必要なもの:

```text
pikasay [options] [message...]

--mood <normal|success|warning|error>  表情・雰囲気を指定
--no-color                            色を無効化
-h, --help                            ヘルプを表示
-v, --version                         バージョンを表示
```

実装技術に応じてオプション解析方法は選択するが、ヘルプ表示とエラー表示の文言は利用者に分かりやすくする。

## 7. 非機能要件

- Node.jsのサポートバージョンを`package.json`とREADMEに明記する。
- インストール後にグローバル実行または`npx pikasay`で利用できる構成にする。
- 実行時に外部通信を行わない。
- 主要な処理は小さなモジュールに分割し、テストしやすくする。
- 日本語入力が文字化けしない。
- 入力が極端に長い場合でも、無制限にメモリを消費しない設計を検討する。
- 終了コードを一貫させる。通常表示は成功、引数・オプションなどの利用エラーは非ゼロとする。

## 8. MVP対象外

次の機能はMVP完成後まで追加しない。

- Git、Docker、テスト結果の自動解析
- AIや外部APIによる文章生成
- アニメーションや常駐プロセス
- 複雑なテーマ・AA編集機能
- 設定ファイルによる大規模なカスタマイズ
- 画像やWeb UI
- 実在のナキウサギ画像の無断利用

## 9. 完了条件

- 新規環境で依存関係をインストールしてビルドできる。
- READMEの例が実際に動作する。
- 自動テストが成功する。
- npmパッケージに必要なファイルだけが含まれる。
- 引数入力とパイプ入力の両方が使える。
- ナキウサギの可愛さと、ネタCLIとしての軽さが伝わる。
