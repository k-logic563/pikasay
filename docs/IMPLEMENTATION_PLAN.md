# pikasay 実装計画

## 進め方

上から順に進める。各フェーズの完了条件を満たしてから次へ進む。
フェーズをまたぐ大きな仕様変更が必要になった場合は、先に`REQUIREMENTS.md`を更新する。

## Phase 0: リポジトリ確認と技術選定

状態: 完了（2026-09-20）

技術選定と調査結果は[`TECHNICAL_DECISIONS.md`](./TECHNICAL_DECISIONS.md)に記録した。

### 作業

- 既存ファイルとGit状態を確認する。
- Node.js、npm、TypeScriptなどの利用可能な環境を確認する。
- 新規リポジトリの場合のディレクトリ構成を決める。
- `package.json`のパッケージ名、bin、scripts、enginesを決める。
- 実行時依存を最小限にする。

### 完了条件

- 採用する構成と理由がREADMEまたは開発メモに残っている。
- `npm install`からビルド・テストまでの流れが定義されている。

## Phase 1: CLIの最小実装

状態: 完了（2026-09-20）

### 作業

- CLIエントリポイントを作る。
- `--help`と`--version`を実装する。
- 引数入力を実装する。
- 標準入力の読み取りを実装する。
- 引数がある場合に標準入力を待たないようにする。
- 通常時のナキウサギAAとメッセージ表示を実装する。

### 完了条件

- `pikasay "こんにちは"`が動作する。
- `echo "こんにちは" | pikasay`が動作する。
- ヘルプとバージョンが動作する。
- 基本の自動テストが成功する。

## Phase 2: 表情・モード・端末表示

状態: 完了（2026-09-20）

### 作業

- normal、success、warning、errorのモードを実装する。
- モードごとのAAまたは表情、鳴き声を定義する。
- 長文と複数行入力の折り返しを実装する。
- ANSIカラーを使う場合は無効化処理を実装する。
- Unicode表示幅による崩れを確認する。

### 完了条件

- 4モードがそれぞれ識別できる。
- 日本語、英語、絵文字を含む入力を確認できる。
- `--no-color`または`NO_COLOR`で色なし表示ができる。
- 端末幅が狭い場合も致命的に崩れない。

## Phase 3: 品質向上とドキュメント

状態: 完了（2026-09-20）

### 作業

- 入力エラーと不正オプションを整える。
- 空入力、空白、改行、長文をテストする。
- READMEにインストール、基本利用、モード、スクリーンショット相当の出力例を追加する。
- npm公開時に含めるファイルを整理する。
- lint、format、test、buildのスクリプトを整える。

### 完了条件

- 自動テストが主要な入力パターンをカバーしている。
- READMEのコマンド例が実装と一致している。
- `npm pack --dry-run`の内容を確認している。

## Phase 4: 公開前検証

状態: 完了・Go（2026-09-25）

MIT License、著作権者、公開用metadata、keywords、READMEの公開情報を確定し、公開予定tarballの作成、クリーン環境へのオフラインインストール、Node.js 22と24でのbuild・test・CLI実行、lint・format、公開ファイル、脆弱性、npmパッケージ名を再検証した。CLI本体とtarballに問題は見つからず、npmパッケージ名`pikasay`も現時点で利用可能と判断した。

GitHubリポジトリ`k-logic563/pikasay`がPublicで、リポジトリとIssuesの公開URLはどちらも200を返すことを確認した。所有者とリポジトリ名は指定どおりで、descriptionは`A cute pika that speaks in your terminal.`と一致する。リモート`main`はローカルHEADと同じコミットを指し、内容もこのプロジェクトと一致するため再利用可能と判断した。ローカルの`origin`も正しいURLを指している。すべての公開前条件を満たし、npm公開を妨げる既知の問題はないため、Phase 4をGoと判定した。

`pikasay@0.1.0`を2026-09-25にnpmへ公開した。npmレジストリから取得したパッケージをNode.js v24.16.0で実行し、引数入力、標準入力、4モード、`--no-color`、`--help`、`--version`が正常に動作することを確認した。npmパッケージURLは<https://www.npmjs.com/package/pikasay>。GitHub側のコミット、push、タグ、Release作成は今回の公開作業では実施していないため、必要に応じて別作業で行う。

### 作業

- クリーンな一時ディレクトリでパッケージをインストールして実行する。
- `npx`相当の利用方法を確認する。
- Node.jsのサポート範囲を確認する。
- パッケージ名の利用可能性とREADME内リンクを確認する。
- ライセンス、著作権表記、公開ファイルを確認する。
- 公開はユーザーの明示的な判断後に行う。

### 完了条件

- クリーンインストールから実行まで成功する。
- 公開対象に不要な開発ファイルや秘密情報がない。
- npm公開前の確認事項がREADMEまたはチェックリストに残っている。

## Phase 5: 公開後の拡張候補

状態: 保留

候補は、MVPの利用状況を見てから優先順位を決める。

- テーマ切り替え
- より豊富なナキウサギの表情
- `--random`による一言や表情のランダム化
- `--json`など機械可読出力。ただしネタCLIの主目的を損なわない範囲に限る
- README用のデモGIFまたは短い動画
- zatsu-lgtmやtsundere-consoleとの連携・紹介

## Codexへの実装指示

各フェーズでは、次の形式で作業する。

1. 対象フェーズの要件を読み直す。
2. 既存コードとテストを確認する。
3. 小さな単位で実装する。
4. テストと手動実行を行う。
5. 完了条件を確認する。
6. このファイルの状態と作業報告を更新する。

ユーザーが特定のフェーズを指定していない場合は、未完了フェーズのうち最も早いものから着手する。
